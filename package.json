{
  "name": "axis-trading",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "pnpm rebuild esbuild",
    "predev": "bash scripts/kill-ports.sh --all",
    "dev": "bash scripts/dev.sh",
    "dev:node": "pnpm predev && pnpm --filter backend dev",
    "dev:dotnet": "pnpm predev && cd backend/donet/TodoApi && dotnet run",
    "dev:python": "pnpm predev && cd backend/python && .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 5555 --reload",
    "db:start": "bash scripts/db.sh",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
